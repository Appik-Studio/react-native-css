name: Lint Commit Messages in Merge Queue (Disabled for fork)

on:
  # Disabled for Appik-Studio fork
  workflow_dispatch: # Manual only

jobs:
  commitlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}

      - uses: wagoid/commitlint-github-action@v6
